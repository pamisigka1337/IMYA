````md
# IMYA — сервис аренды брендовой одежды

**IMYA** — fullstack веб-приложение для аренды дизайнерской одежды.  
Пользователь может просматривать каталог брендовых вещей, фильтровать товары, выбирать даты аренды, бронировать одежду и оформлять заказ.

Проект реализован с использованием **React + Node.js + PostgreSQL**.

---

## Возможности

### Каталог одежды
- просмотр всех доступных товаров
- карточки брендов
- цены аренды
- размеры

### Фильтрация
- по категории
- по бренду
- по размеру
- по цене

### Карточка товара
- подробное описание
- фотографии
- бренд
- размер
- стоимость аренды
- залог

### Бронирование
- выбор даты начала аренды
- выбор даты окончания
- автоматический расчет стоимости

### Оформление заказа
- расчет итоговой суммы
- учет залога
- подтверждение бронирования

### Личный кабинет
- просмотр заказов
- статус бронирования

### Админ-панель
- управление заказами
- просмотр бронирований

---

## Технологический стек

### Frontend
- React
- Vite
- TypeScript
- Tailwind CSS
- shadcn/ui

### Backend
- Node.js
- Express
- TypeScript

### База данных
- PostgreSQL
- Drizzle ORM

---

## Архитектура проекта

```bash
IMYA/
│
├── client/              # frontend приложения
├── server/              # backend API
│   ├── index.ts
│   ├── routes.ts
│   └── ...
├── shared/              # общие типы и схемы
├── scripts/             # служебные скрипты
├── drizzle.config.ts
├── vite.config.ts
├── package.json
└── README.md
````

---

## Структура базы данных

### `users`

* `id`
* `email`
* `password`
* `role`
* `created_at`

### `items`

* `id`
* `name`
* `brand`
* `category`
* `size`
* `price_per_day`
* `deposit`
* `description`
* `image`

### `bookings`

* `id`
* `user_id`
* `item_id`
* `start_date`
* `end_date`
* `status`
* `total_price`
* `created_at`

---

## API endpoints

| Метод | Endpoint         | Описание                                |
| ----- | ---------------- | --------------------------------------- |
| GET   | `/api/items`     | Получить каталог товаров                |
| GET   | `/api/items/:id` | Получить информацию о конкретном товаре |
| POST  | `/api/bookings`  | Создать бронирование                    |
| GET   | `/api/bookings`  | Получить бронирования пользователя      |

---


## Что нужно скачать перед запуском

Для локального запуска проекта установи:

| Что скачать | Зачем нужно | Ссылка |
| --- | --- | --- |
| **Node.js LTS** | Запускает backend и frontend, вместе с ним устанавливается `npm` | [https://nodejs.org/en/download](https://nodejs.org/en/download) |
| **Git** | Чтобы скачать проект с GitHub через `git clone` | [https://git-scm.com/install](https://git-scm.com/install) |
| **PostgreSQL** | База данных для пользователей, товаров и заказов | [https://www.postgresql.org/download/](https://www.postgresql.org/download/) |

После установки проверь в терминале:

```bash
node -v
npm -v
git --version
psql --version
```

Если все команды показывают версии без ошибок, можно переходить к запуску проекта.

---

## Как запустить проект

### 1. Клонировать репозиторий

```bash
git clone https://github.com/pamisigka1337/IMYA.git
cd IMYA
```

### 2. Установить зависимости

```bash
npm install
```

### 3. Настроить переменные окружения

Создай файл `.env` в корне проекта и добавь:

```env
DATABASE_URL=postgres://postgres:password@localhost:5432/imya
PORT=5000
NODE_ENV=development
```

### 4. Убедиться, что запущен PostgreSQL

Перед запуском проекта база данных PostgreSQL должна быть активна.

Также нужно создать базу данных, если она еще не создана:

```sql
CREATE DATABASE imya;
```

### 5. Применить миграции

В зависимости от того, как настроен проект, выполни одну из команд:

```bash
npm run db:push
```

или

```bash
npm run db:migrate
```

### 6. Запустить проект

```bash
npm run dev
```

После запуска приложение будет доступно по адресу:

```bash
http://localhost:5000
```

---

## Требования для запуска

Перед установкой убедись, что у тебя установлены:

* **Node.js** 18+
* **npm** 9+
* **PostgreSQL** 14+

---

## Безопасность

В проекте используются:

* **Drizzle ORM** для защиты от SQL Injection
* серверная валидация данных
* разделение frontend и backend
* переменные окружения для конфиденциальных данных

---

## Возможные улучшения

* интеграция онлайн-оплаты
* система отзывов
* загрузка изображений
* авторизация через JWT
* email-уведомления
* календарь занятости вещей
* панель аналитики для администратора

---

## Демонстрация проекта

Основные страницы:

* Главная
* Каталог одежды
* Карточка товара
* Оформление заказа
* Личный кабинет

---

## Автор

**Разработчик:** pamisigka1337

GitHub:
[https://github.com/pamisigka1337](https://github.com/pamisigka1337)

```

Что еще я бы улучшил именно для GitHub:
1. добавить скриншоты интерфейса;
2. добавить раздел `Scripts` с командами вроде `npm run dev`, `npm run build`, `npm run db:migrate`;
3. написать, используется ли одна команда для запуска и клиента, и сервера одновременно;
4. добавить статус проекта: `в разработке` или `готов к демонстрации`.

Могу сразу сделать тебе еще более сильную версию README в стиле “проект для портфолио / резюме”, чтобы он выглядел как готовый коммерческий pet-project.
```

---

## Подключение домена через редирект

Чтобы сайт открывался на твоем домене, а старый адрес автоматически переводил пользователей на новый домен:

1. Собери и запусти проект в production-режиме:

```bash
npm run build
npm run start
```

2. В настройках хостинга добавь переменную окружения `PUBLIC_SITE_URL` со своим доменом:

```env
PUBLIC_SITE_URL=https://example.com
```

Можно указать домен без протокола, например `example.com` — сервер сам будет использовать `https://`.

3. Настрой DNS домена у регистратора на адрес твоего хостинга. После этого любые запросы со старого домена или технического адреса хостинга будут перенаправляться на `PUBLIC_SITE_URL` с сохранением пути страницы, например `/catalog` или `/item/1`.

Для локальной разработки редирект не мешает: запросы на `localhost`, `127.0.0.1`, `0.0.0.0` и `::1` не перенаправляются.
